#!/bin/bash
# Build and run RadioBar — menubar app for controlling the Roberts Radio
set -euo pipefail

DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$DIR"

swift build -c release --quiet 2>&1
exec .build/release/RadioBar "$@"
